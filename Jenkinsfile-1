pipeline {
    agent any
    
    // Commented out the empty tools section
    // tools {
    //     // Remove this entire block if no tools are configured
    // }
    
    environment {
        // Define scanner tool from Global Tool Configuration
        scannerHome = tool 'SonarScanner'
    }
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv(installationName: 'My Sonar Server') {
                    // For Maven project
                    sh 'mvn clean verify sonar:sonar'
                }
            }
        }
        
        // Optional: Add Quality Gate check
        stage('Quality Gate') {
            steps {
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }
    }
    
    post {
        always {
            echo 'SonarQube analysis completed!'
        }
        success {
            echo 'Pipeline succeeded! Check SonarQube dashboard.'
        }
        failure {
            echo 'Pipeline failed. Check Jenkins logs.'
        }
    }
}
